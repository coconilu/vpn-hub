# Mihomo 开发链路首轮验证

日期：2026-07-18
平台：Windows x64
结论：开发链路通过，未触碰受保护的 `6666`；出口本身仍存在海外目标超时。

## Sidecar

| 项目 | 观测值 |
|---|---|
| 来源 | MetaCubeX/mihomo 官方 GitHub Release |
| 版本 | `v1.19.28` |
| 架构 | Windows AMD64 |
| SHA-256 | `27bdbd8f476dfb0f65a2a8ecf43cdf7edc0a132326efc7660308a1302c034a20` |
| 配置验证 | `mihomo -t` 通过 |

## 链路

```text
显式 curl → 127.0.0.1:36666 (Mihomo)
          → OUT-LOCAL-A
          → 127.0.0.1:16666 (chaoshihuiCore)
```

| 验证 | 结果 |
|---|---|
| Mihomo 监听 | `127.0.0.1:36666`，PID 与受控会话一致 |
| 国内 HTTPS 目标 | HTTP 200，约 81ms |
| 海外探测目标 | TLS 连接超时，与直接通过 `16666` 的结果一致 |
| Windows 系统代理 | 全程保持 `127.0.0.1:6666` |
| `6666` 所有者 | 测试前后均为原 `SpeedCatCore.exe` PID |
| 开发实例停止 | Ctrl+C 正常关闭，`36666` 随后释放 |
| 第三方客户端 | `16666` 与 `6666` 全程保持监听 |

## 解释

国内目标成功证明了双层 SOCKS5/Mixed 代理链路成立；海外目标失败与上游直接探测完全一致，说明 Mihomo 没有通过 `DIRECT` 绕过本地客户端 A。当前故障属于上游节点或路由质量，而不是开发编排层端口冲突。

## 待办

- [ ] 在本地客户端 A 切换到可用海外节点后复测海外目标。
- [ ] 增加多目标探测，区分国内连通、海外连通和目标站单点故障。
- [ ] 验证 UDP；当前开发配置显式设置 `udp: false`。
- [ ] 客户端 B 迁移到 `26666` 后加入同一开发拓扑。
